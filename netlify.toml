[build]
  command = "pip install -r requirements.txt && python manage.py collectstatic"
  publish = "staticfiles"
  functions = "functions"

[dev]
  functions = "functions"
  targetPort = 5000
  command = "python app.py"

[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200